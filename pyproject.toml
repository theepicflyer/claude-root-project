[project]
name = "root-law-scraper"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "lxml>=6.1.1",
    "pyyaml>=6.0.3",
    "requests>=2.34.2",
]
